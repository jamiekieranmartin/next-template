import { TeamLayout, TeamLayoutProps } from "../../components/team-layout";

export default {
  title: "Components/TeamLayout",
  component: TeamLayout,
  args: {
    children: "TeamLayout",
  },
};

// export const Default = (args: TeamLayoutProps) => <TeamLayout {...args} />;
